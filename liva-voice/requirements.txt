# LIVA 2.0 - Voice Cloning Pipeline
# Requirements

# API Server (liva_api.py)
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
pydantic>=2.7.0

# TTS - Edge (cloud voice, optional)
edge-tts>=6.1.0

# Core ML
torch>=2.0.0
torchaudio>=2.0.0

# Audio Processing
yt-dlp>=2024.0.0
ffmpeg-python>=0.2.0

# STT - Speech to Text
faster-whisper>=1.0.0

# VAD - Voice Activity Detection
silero-vad>=0.5.0

# Audio Enhancement
deepfilternet>=0.3.0

# Speaker Verification
speechbrain>=1.0.0

# Vietnamese NLP
num2words>=0.5.0

# Utilities
numpy>=1.24.0
scipy>=1.11.0
librosa>=0.10.0

# Async support
aiofiles>=23.0.0

# Inference runtime — GPU mặc định (máy dev có RTX); nếu không có GPU NVIDIA,
# thay bằng dòng CPU bên dưới (KHÔNG cài cả hai — xung đột package)
onnxruntime-gpu>=1.16.0
# onnxruntime>=1.16.0
