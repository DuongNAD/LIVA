name: LIVA H-MEM Test Suite CI

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  test-gateway:
    runs-on: windows-latest
    env:
      LIBCLANG_PATH: 'C:\Program Files\LLVM\bin'

    steps:
    - name: Checkout Code
      uses: actions/checkout@v4

    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '22'
        cache: 'npm'

    # Chỉ dùng thư viện chuẩn của Node — không cần `npm ci`, nên đặt sớm để fail nhanh.
    # Gate: front-matter thiếu/sai, liên kết hỏng, `covers` trỏ tới file không tồn tại,
    # hai tài liệu cùng nhận sở hữu một sự thật. Tài liệu lỗi thời chỉ CẢNH BÁO, không chặn.
    - name: Check Documentation
      run: node scripts/docs-check.mjs

    - name: Install Dependencies
      run: npm ci

    - name: Install LLVM
      run: choco install llvm -y

    - name: Run UI Tests
      run: npm run test -w liva-ui

    - name: Run Native Core Tests
      working-directory: liva-native-core
      run: cargo test

    # Clippy is informational for now (~66 lib warnings as of 2026-07): it
    # surfaces lint regressions in the logs without gating merges. Once the
    # count is driven to zero, drop `continue-on-error` and add `-- -D warnings`
    # to make it a hard gate.
    - name: Clippy (non-blocking)
      working-directory: liva-native-core
      run: cargo clippy --all-targets
      continue-on-error: true

